# Core packages for the playground.
# The official TRIBE v2 package itself pins the rest of its runtime dependencies.
torch>=2.5.1,<2.7
numpy==2.2.6
torchvision>=0.20,<0.22
matplotlib
pandas

# Official Meta Research TRIBE v2 package
git+https://github.com/facebookresearch/tribev2.git
